/// <reference types="vite/client" />

// CSS-only export from @unicitylabs/sphere-ui — Vite handles the import at
// runtime, but `tsc -b` doesn't know how to resolve a CSS exports map entry.
declare module '@unicitylabs/sphere-ui/styles';
